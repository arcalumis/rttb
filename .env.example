# =============================================================================
# Environment Configuration
# =============================================================================
# Copy this file to .env for local development or .env.production for production

# =============================================================================
# Required - Core Settings
# =============================================================================

# Replicate API token for image generation
# Get yours at: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_your_token_here

# JWT secret for signing authentication tokens
# Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string

# Initial admin password (change after first login)
ADMIN_PASSWORD=admin

# Encryption key for storing user API keys securely
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your-32-byte-hex-key

# =============================================================================
# Production Settings (only needed in .env.production)
# =============================================================================

# Set to 'production' for production deployment
# NODE_ENV=production

# Server port (default: 3001)
# PORT=3001

# Your domain name (required for SSL certificates)
# DOMAIN=app.example.com

# =============================================================================
# Optional - S3-Compatible Storage (for image storage)
# =============================================================================
# Use DigitalOcean Spaces, Cloudflare R2, or AWS S3

# S3_ENDPOINT=https://nyc3.digitaloceanspaces.com
# S3_BUCKET=your-bucket-name
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_REGION=nyc3

# =============================================================================
# Email Settings (Resend)
# =============================================================================

# Resend API key for sending emails
# Get yours at: https://resend.com/api-keys
# RESEND_API_KEY=re_your_api_key

# Email sender address (must be verified in Resend or use onboarding@resend.dev for testing)
# FROM_EMAIL=noreply@yourdomain.com

# App URL for links in emails (used in magic link and password reset emails)
# APP_URL=http://localhost:5173

# =============================================================================
# Email Authentication Settings
# =============================================================================

# Magic link token expiration in minutes (default: 15)
# MAGIC_LINK_EXPIRY_MINUTES=15

# Password reset token expiration in minutes (default: 60)
# PASSWORD_RESET_EXPIRY_MINUTES=60

# =============================================================================
# Stripe Billing Settings
# =============================================================================
# For subscription billing and payments

# Stripe secret key (from Dashboard > Developers > API keys)
# STRIPE_SECRET_KEY=sk_test_...

# Stripe webhook secret (from Dashboard > Developers > Webhooks)
# STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe publishable key (for frontend - optional, can be hardcoded)
# STRIPE_PUBLISHABLE_KEY=pk_test_...

# =============================================================================
# Development Settings
# =============================================================================
# These are only used in local development

# Frontend dev server URL (for CORS)
# FRONTEND_URL=http://localhost:5173
